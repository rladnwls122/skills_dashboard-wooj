# CONTEXT

이 저장소의 용어집. 구현 세부는 적지 않는다.

## 채점 창 (scoring window)

경기 시작 T로부터 **T+1h ~ T+3h**, 트래픽이 발생하는 2시간. 비용은 이 창 안에서만 집계되어 채점에 반영된다.
구축 시간(T ~ T+1h)에 노드를 몇 대 띄웠든 비용 점수에는 들어가지 않는다.

"대회 시간"과 구분할 것 — 대회는 3시간이고 채점 창은 그중 2시간이다.

## 비정상 요청 (abnormal request)

**제공하는 API 경로**로 들어온 변조·공격성 요청. **403**으로 응답해야 하며, WAF `Block` 액션이 낸다.

## 미지정 경로 (undefined path)

제공하는 API에 없는 경로로 들어온 요청. **404**로 응답해야 한다. **Block이 아니다** —
WAF가 Block하면 403이 나가서 요구사항을 위반한다. 라우팅 계층의 고정 응답이나 앱이 낸다.

따라서 모든 차단 규칙에는 **경로 스코프다운**이 필요하다: 제공 API 경로일 때만 Block.

## 오탐 (false block)

정상 요청이 403을 맞은 것. 가용성 점수에 직결되는 사고이며, 차단 규칙을 넣은 뒤 가장 먼저 감시해야 할 값이다.

## 채점기 입력값 (grader input)

우리가 관측할 수 있고 통제할 수 있는 값 — 채점 창 기준 평균 인스턴스 대수, SLO 달성률, 규격 외 인스턴스 유무.
**채점 점수 자체와 구분한다.** 점수는 비공개 절대 계산식으로 산출되므로 우리 화면은 점수를 만들어내지 않고
입력값만 정확히 센다.

## 규격 외 인스턴스 (off-spec instance)

과제가 허용한 것(`t3.medium`, 지정 리전 `ap-northeast-2`, 지정 DB 클래스) 밖의 인스턴스, 또는 미사용 인스턴스.
존재 자체가 패널티 사유다.

## 승격 (promotion)

WAF 규칙을 `COUNT`로 먼저 적용해 실제 트래픽에서 몇 건이 걸리는지 실측한 뒤, 오탐이 없음을 확인하고
`BLOCK`으로 올리는 것. 로컬 시뮬레이션을 대체하는 검증 절차다.
