node_modules/
.next/
data/
*.db
.env
.env.local
# generate-env.sh writes a timestamped backup next to .env; it holds the same
# keys and must never be committed.
.env.bak.*
next-env.d.ts
*.tsbuildinfo
backend/bin/

# Release build copies the built frontend here to embed it; not committed.
backend/webdist/

# The source build drops the backend binary in the repository root.
api.exe
api
